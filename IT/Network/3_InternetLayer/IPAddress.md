___
___
# Tags
#network
___
# Содержание
- [[#1. IPv4]]
	- [[#1.1. Маска подсети]]
		- [[#1.1.1. Вычисление адреса подсети в двоичном виде]]
		- [[#1.1.2 подсчет количества адресов в подсети]]
	- [[#1.2. Классификация подсетей]]
	- [[#1.3. Исключительные (зарезервированные адреса)]]
	- [[#1.4. Структура IP-пакета]]
- [[#2. IPv6]]
	- [[#2.1. Формат записи]]
		- [[#2.1.1. Правила сокращения]]
	- [[#2.2. Структура адреса и префиксы]]
	- [[#2.3. Типы адресов IPv6]]
	- [[#2.4. Структура IPv6-пакета]]
	- [[#2.5. Механизм Neighbor Discovery (NDP)]]
___
# 1. IPv4
*Здесь речь идет только об IPv4*
**IP** – 4-х байтовый виртуальный адрес.

IP-адрес состоит из 4-х "октет", каждая по байту:
![](IPv4)
## 1.1. Маска подсети
### 1.1.1. Вычисление адреса подсети в двоичном виде
IP-адрес
```
10101100100111010101111011100001
```
& Маска подсети
```
11111111111111111111111100000000 |<- тут количество старших бит определяется числом после /24
```
= Адрес посети
```
10101100100111010101111000000000
```
### 1.1.2 подсчет количества адресов в подсети
Имеем, например, адрес с маской `172.157.94.225/24`
Всего имеется 32 бита (4 байта). Вычитаем из общего количества (32) число после слеша (24):$$32-24=8$$
У нас остается 8 последних бит, которые можно распределить в качестве адресов хостов. Подсчитаем количество:$$2^8=256$$
Именно столько (256) адресов хостов можно получить в данной подсети.

Имеется нюанс, заключающийся в том, что фактически адресов хостов будет всегда минимум на 2 меньше, так как:
- `xxx.xxx.xxx.000` - резерв, так как является, собственно, адресом подсети;
- `xxx.xxx.xxx.255` - широковещательный IP-адрес в своей подсети
## 1.2. Классификация подсетей
| Класс | Первые биты | Диапазон первого октета | Маска | Хостов в сети   |
| ----- | ----------- | ----------------------- | ----- | --------------- |
| **A** | `0...`      | 1 – 126                 | /8    | ~16 млн         |
| **B** | `10...`     | 128 – 191               | /16   | ~65 тыс         |
| **C** | `110...`    | 192 – 223               | /24   | 254             |
| **D** | `1110...`   | 224 – 239               | —     | Мультикаст      |
| **E** | `1111...`   | 240 – 255               | —     | Зарезервировано |
## 1.3. Исключительные (зарезервированные адреса)

| Адрес             | Назначение                                                       |
| ----------------- | ---------------------------------------------------------------- |
| `255.255.255.255` | Широковещательный IP-адрес в своей подсети                       |
| `172.157.94.255`  | Широковещательный IP-адрес для конкретной подсети `172.157.94.0` |
| `172.157.94.0`    | Адрес подсети, тут нет хоста                                     |
| `0.0.0.0`         | IP перед получением реального собственного у провайдера          |
| `127.0.0.0/8`     | Ведет на наш компьютер                                           |
## 1.4. Структура IP-пакета
![](IPPacket.jpg)
- Поле _Номер версии_ (VERS) указывает версию протокола IP. Сейчас повсеместно используется версия 4 и уже начался переход на версию 6, называемую также IPng (IP next generation) или IPv6.
- Поле _Длина заголовка_ (HLEN) пакета IP занимает 4 бита и указывает значение длины заголовка, измеренное в 32-битовых словах. Обычно заголовок имеет длину в 20 байтов (пять 32-битовых слов), но при увеличении объема служебной информации эта длина может быть увеличена за счет использования дополнительных байтов в поле Резерв (IP OPTIONS).
- Поле _Тип сервиса_ (SERVICE TYPE) занимает 1 байт и задает приоритетность пакета и вид критерия выбора маршрута. Первые три бита этого поля образуют подполе приоритета пакета (PRECEDENCE). Приоритет может иметь значения от 0 (нормальный пакет) до 7 (пакет управляющей информации). Маршрутизаторы и компьютеры могут принимать во внимание приоритет пакета и обрабатывать более важные пакеты в первую очередь. Поле Тип сервиса содержит также три бита, определяющие критерий выбора маршрута. Установленный бит D (delay) говорит о том, что маршрут должен выбираться для минимизации задержки доставки данного пакета, бит Т − для максимизации пропускной способности, а бит R − для максимизации надежности доставки.
- Поле _Общая длина_ (TOTAL LENGTH) занимает 2 байта и указывает общую длину пакета с учетом заголовка и поля данных.
- Поле _Идентификатор пакета_ (IDENTIFICATION) занимает 2 байта и используется для распознавания пакетов, образовавшихся путем фрагментации исходного пакета. Все фрагменты должны иметь одинаковое значение этого поля.
- Поле _Флаги_ (FLAGS) занимает 3 бита, оно указывает на возможность фрагментации пакета (установленный бит Do not Fragment − DF запрещает маршрутизатору фрагментировать данный пакет), а также на то, является ли данный пакет промежуточным или последним фрагментом исходного пакета (установленный бит More Fragments — MF говорит о том, что пакет переносит промежуточный фрагмент).
- Поле _Смещение фрагмента_ (FRAGMENT OFFSET) занимает 13 битов, оно используется для указания в байтах смещения поля данных этого пакета от начала общего поля данных исходного пакета, подвергнутого фрагментации. Используется при сборке/разборке фрагментов пакетов при передачах их между сетями с различными величинами максимальной длины пакета.
- Поле _Время жизни_ (TIME ТО LIVE) занимает 1 байт и указывает предельный срок, в течение которого пакет может перемещаться по сети. Время жизни данного пакета измеряется в секундах и задается источником передачи средствами протокола IP. На шлюзах и в других узлах сети по истечении каждой секунды из текущего времени жизни вычитается единица; единица вычитается также при каждой транзитной передаче (даже если не прошла секунда). По истечении времени жизни пакет аннулируется.
- Идентификатор _Протокола верхнего уровня_ (PROTOCOL) занимает 1 байт и указывает какому протоколу верхнего уровня принадлежит пакет (например, это могут быть протоколы TCP, UDP или RIP).
- _Контрольная сумма заголовка_ (HEADER CHECKSUM) занимает 2 байта; она рассчитывается по всему заголовку.
- Поля _Адрес источника_ (SOURCE IP ADDRESS) и _Адрес назначения_ (DESTINATION IP ADDRESS) имеют одинаковую длину − по 32 бита и идентичную структуру.
- Поле _Резерв_ (IP OPTIONS) является необязательным и используется обычно только при отладке сети. Это поле состоит из нескольких подполей, каждое из которых может быть одного из восьми предопределенных типов. В этих подполях можно указывать точный маршрут прохождения маршрутизаторов, регистрировать проходимые пакетом маршрутизаторы, помещать данные системы безопасности, а также временные отметки. Поскольку число подполей может быть произвольным, то в конце поля Резерв должно быть добавлено несколько байтов для выравнивания заголовка пакета по 32-битовой границе.
___
# 2. IPv6
**IPv6** — новая версия протокола IP, разработанная для решения проблемы дефицита адресов IPv4 и упрощения обработки пакетов маршрутизаторами.

**Ключевое отличие:** адрес имеет длину **128 бит** (16 байт), что дает $2^{128}$ адресов (примерно $3.4 \times 10^{38}$).
## 2.1. Формат записи
В отличие от десятичных октетов IPv4, IPv6 записывается в **шестнадцатеричном** виде. Адрес делится на 8 блоков по 16 бит (4 шестнадцатеричные цифры), разделенных двоеточием: 
`2001:0db8:85a3:0000:0000:8a2e:0370:7334`
### 2.1.1. Правила сокращения
Для удобства записи применяются два правила:
1. **Удаление ведущих нулей:** в каждом блоке можно не писать нули в начале.
    - `0db8` -> `db8`, `0000` -> `0`.
2. **Двойное двоеточие (`::`):** одна или несколько подряд идущих групп с нулевым значением могут быть заменены на `::`.
    - _Важно:_ Это сокращение можно использовать в адресе **только один раз**.
    - Пример: `2001:db8:85a3:0:0:8a2e:370:7334` -> `2001:db8:85a3::8a2e:370:7334`.
## 2.2. Структура адреса и префиксы
В IPv6 нет масок подсети в привычном виде (как `255.255.255.0`), используется только CIDR-нотация.
- **Префикс (Network Prefix):** Первые 64 бита. Определяют сеть и подсеть.
- **Идентификатор интерфейса (Interface ID):** Последние 64 бита. Уникальный адрес хоста внутри сети (часто формируется на основе MAC-адреса по методу EUI-64).
## 2.3. Типы адресов IPv6
В IPv6 **нет широковещательных адресов (Broadcast)**. Вместо них используется Multicast.

|**Тип адреса**|**Диапазон / Префикс**|**Описание**|
|---|---|---|
|**Global Unicast**|`2000::/3`|Глобальные адреса, маршрутизируемые в интернете (аналог белых IPv4).|
|**Link-Local**|`fe80::/10`|Автоматически назначаются на каждом интерфейсе. Работают только в пределах одного сегмента сети (L2).|
|**Unique Local (ULA)**|`fc00::/7`|Аналог "серых" адресов (192.168.x.x). Для внутренних сетей организаций.|
|**Multicast**|`ff00::/8`|Групповая рассылка. Пакет доставляется всем членам группы.|
|**Loopback**|`::1/128`|Обращение к самому себе (аналог `127.0.0.1`).|
|**Unspecified**|`::/128`|Неопределенный адрес (аналог `0.0.0.0`).|
## 2.4. Структура IPv6-пакета
Заголовок IPv6 стал фиксированным (**40 байт**), что значительно ускоряет его обработку маршрутизаторами.
**Основные поля:**
- **Version (4 бита):** Всегда `6`.
- **Traffic Class (8 бит):** Аналог Type of Service в IPv4. Используется для QoS (приоритезации трафика).
- **Flow Label (20 бит):** Маркировка потока данных для сохранения последовательности пакетов.
- **Payload Length (16 бит):** Размер данных, следующих за заголовком (до 64 КБ).
- **Next Header (8 бит):** Указывает тип следующего заголовка (TCP, UDP или расширения IPv6).
- **Hop Limit (8 бит):** Аналог TTL в IPv4. При каждом проходе через маршрутизатор уменьшается на 1.
- **Source / Destination Address (по 128 бит):** Адреса отправителя и получателя.
## 2.5. Механизм Neighbor Discovery (NDP)
Поскольку в IPv6 нет ARP (который работал через Broadcast), используется протокол **NDP**:
1. **Router Solicitation (RS):** Хост ищет маршрутизатор. 
2. **Router Advertisement (RA):** Маршрутизатор сообщает параметры сети (автонастройка SLAAC).
3. **Neighbor Solicitation (NS):** Поиск MAC-адреса соседа по его IP.
4. **Neighbor Advertisement (NA):** Ответ с MAC-адресом.
> [!note] 
> **SLAAC (Stateless Address Autoconfiguration)** — киллер-фича IPv6. Хост может сам сгенерировать себе валидный IP-адрес, просто узнав префикс от маршрутизатора, без использования DHCP-сервера. 

___
