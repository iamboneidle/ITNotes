___
___
# Tags
#kubernetes
___
# Содержание
- [[#1. Зачем нужно]]
- [[#2. Использование набора демонов для запуска на каждом узле]]
- [[#3 Использование набора демонов для запуска модуля только на определенных узлах]]
    - [[#3.1. Создание YAML-определения ресурса DaemonSet]]
- [[#Литература]]
___
# 1. Зачем нужно
DaemonSet позволяет запускать ровно по одному модулю на каждом узле. Такая необходимость возникает в тех случаях, когда необходимо развернуть модули, обеспечивающие работу инфраструктуры.
Например, требуется на каждом узле запустить сборщик логов и монитор ресурсов. Еще одним хорошим примером является собственный процесс `kube-proxy` системы Kubernetes, который должен работать на всех узлах, чтобы заставлять службы работать.

За пределами Kubernetes такие процессы обычно запускаются с init-скриптов или демона `systemd` во время начальной загрузки узла. На узлах Kubernetes вы по-прежнему можете использовать `systemd` для запуска системных процессов, но тогда вы не сможете воспользоваться всеми возможностями, которые предоставляет Kubernetes.
___
# 2. Использование набора демонов для запуска на каждом узле
Для запуска модуля на всех узлах кластера создается объект DaemonSet, который похож на объекты ReplicationController или ReplicaSet, за исключением
того, что модули, созданные набором демонов, уже имеют заданный целевой узел и пропускают планировщик Kubernetes.
___
# 3 Использование набора демонов для запуска модуля только на определенных узлах
Набор демонов развертывает модули на всех узлах кластера, если не указано, что модули должны выполняться только на подмножестве всех узлов. Это
делается путем задания свойства nodeSelector в шаблоне модуля, который
является частью определения ресурса DaemonSet.
## 3.1. Создание YAML-определения ресурса DaemonSet
```yaml
apiVersion: apps/v1beta2 # |<- объект DaemonSet находится в группе apps API
kind: DaemonSet          # |__ версии v1beta2
metadata:
  name: ssd-monitor
spec:
  selector:
    matchLabels:
      app: ssd-monitor
  template:
    metadata:
      labels:
        app: ssd-monitor
    spec:
      nodeSelector: # |<- шаблон модуля включает в себя селектор узлов,
        disk: ssd   # |__ который отбирает узлы с меткой disk=ssd
      containers:
        – name: main
          image: luksa/ssd-monitor
```
Создание набора демонов делается командой 
```shell
kubectl create <file>.yaml
```
Убедитесь, что узлы помечены меткой `disk=ssd`, если нет, то можно пометить так:
Вывод списка узлов:
```shell
kubectl get node
```
теперь ставим метки:
```shell
kubectl lable node <node_name> disk-ssd
```
___
# Литература
- Марко Лукша – Kubernetes в действии
