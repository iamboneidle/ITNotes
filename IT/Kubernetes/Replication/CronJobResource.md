___
___
# Tags
#kubernetes
___
# Содержание
- [[#1. Планирование выполнения заданий периодически или один раз в будущем]]
- [[#2. Создание ресурса CronJob]]
	- [[#2.1. Настройка расписания]]
- [[#3. Общие сведения о выполнении запланированных заданий]]
- [[#Литература]]
___
# 1. Планирование выполнения заданий периодически или один раз в будущем
Ресурсы Job запускают свои модули немедленно при создании ресурса Job. Но
многие пакетные задания должны выполняться в определенное время в будущем или повторно в заданном интервале. В Linux- и UNIX-подобных операционных системах эти задания более известны как задания cron. Kubernetes их тоже поддерживает.
___
# 2. Создание ресурса CronJob
Будем запускать задание каждые 15 минут:
```yaml
apiVersion: batch/v1beta1
kind: CronJob
metadata:
  name: batch-job-every-fifteen-minutes
spec:
  schedule: "0,15,30,45 * * * *" # |<- **выполняться на 0, 15, 30 и 45 минутах каждого часа каждого дня
  jobTemplate:
    spec:
      template:                      # |<- Шаблон ресурса Job для запуска 
        metadata:                    # | по хрону cron
          labels:                    # |
            app: periodic-batch-job  # |
        spec:                        # |
          restartPolicy: OnFailure   # |
          containers:                # |
            – name: main             # |
              image: luksa/batch-job # |__
```
## 2.1. Настройка расписания
Слева направо расписание содержит следующие пять записей:
- минута;
- час;
- день месяца;
- месяц;
- день недели.
В этом примере требуется запускать задание каждые 15 минут, поэтому расписание должно быть `«0,15,30,45 * * * *»`, что означает на отметке в 0, 15, 30 и 45 минут каждого часа (первая звездочка), каждого дня месяца (вторая звездочка), каждого месяца (третья звездочка) и каждого дня недели (четвертая звездочка).
Если бы вместо этого требовалось, чтобы оно запускалось каждые 30 минут,
но только в первый день месяца, то вы бы установили расписание 
`«0,30 * 1 * *»`, и если требуется, чтобы оно работало в 3 часа ночи каждое воскресенье, то вы установите `«0 3 * * 0»` (последний ноль означает воскресенье).

> [!info] 
> [[Cron|Подробнее тут]] 

___
# 3. Общие сведения о выполнении запланированных заданий
Ресурсы Job будут создаваться из ресурса CronJob приблизительно в запланированное время. Затем Job создает модули.

Может случиться так, что задание или модуль создается и выполняется относительно поздно. У вас может быть жесткое требование, чтобы задание не было запущено слишком отдаленно в течение запланированного времени. В этом случае можно указать крайний срок, задав поле `startingDeadlineSeconds` в
секции `spec` ресурса CronJob, как показано в следующем ниже списке.
```yaml
apiVersion: batch/v1beta1
kind: CronJob
spec:
  schedule: "0,15,30,45 * * * *"
  startingDeadlineSeconds: 15 # |<- 
# ______________________________|  
# | модуль должен начать работу
# | не позднее чем верез 15 секунд
# | после запланированного времени
# |__
...
```
Если по какой-либо причине ко времени xx:xx:15 оно не запустится, задание не будет запущено и будет показано как `Failed`.
В обычных условиях CronJob всегда создает всего одно задание для каждого
выполнения, настроенного в расписании, но может случиться так, что два задания создаются одновременно или вообще не создаются. 
Для борьбы с первой проблемой ваши задания должны быть идемпотентными (их выполнение несколько раз, вместо одного раза, не должно привести к нежелательным результатам). 
Что касается второй проблемы, то убедитесь, что следующий запуск задания выполняет любую работу, которая должна была быть выполнена предыдущим (пропущенным) запуском.
___
# Литература
- Марко Лукша – Kubernetes в действии
