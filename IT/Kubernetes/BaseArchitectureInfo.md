___
___
# Tags

#kubernetes 
___
# Содержание
- [[#1. Control plane]]
- [[#2. Nodes]]
- [[#3. Как k8s запускает контейнеры с помощью описания]]
- [[#4. Обеспечение бесперебойной работы контейнеров]]
- [[#5. Services]]
- [[#6. Контроллер репликации]]
- [[#Литература]]
___
Kubernetes cluster состоит из множества узлов, которые можно разделить на два вида:
- _ведущий_ узел (мастер), на котором размещена _плоскость управления_ _(Control Plane)_ _Kubernetes_, контролирующая и управляющая всей системой Kubernetes;
- рабочие _узлы_, на которых выполняются развертываемые приложения.
___
# 1. Control plane
Плоскость управления – это то, что управляет кластером и заставляет его функционировать. Она состоит из нескольких компонентов, которые могут работать на одном ведущем узле либо быть распределены по нескольким узлам и реплицированы для обеспечения высокой доступности. Эти компоненты следующие:
- _Kubernetes API сервер_, с которым взаимодействуете вы и другие компоненты плоскости управления;
- _Scheduler (планировщик)_, который распределяет приложения (назначает рабочий узел каждому развертываемому компоненту приложения);
- _Controller-Manager (менеджер контроллеров)_, выполняющий функции кластерного уровня, такие как репликация компонентов, отслеживание рабочих узлов, обработка аварийных сбоев узлов и т. д.;
- _etcd_, надежное распределенное хранилище данных, которое непрерывно сохраняет конфигурацию кластера.
Компоненты плоскости управления содержат и управляют состоянием кластера, но не выполняют приложения. Это делается (рабочими) узлами.
___
# 2. Nodes
Рабочие узлы – это машины, на которых выполняются контейнеризированные приложения. Задача выполнения, мониторинга и предоставления служб приложениям выполняется следующими компонентами:
- _Container Runtime_: Docker, rkt или другая среда выполнения контейнеров_, в которой выполняются контейнеры;
- _Kubelet_, агент, который обменивается с сервером API и управляет контейнерами на своем узле;
- _Kube-proxy (служебный прокси Kubernetes)_, который балансирует нагрузку сетевого трафика между компонентами приложения.
___
# 3. Как k8s запускает контейнеры с помощью описания
Сервер Kubernetes API обрабатывает описание приложения 
-> 
Планировщик назначает указанные группы контейнеров доступным узлам, исходя из русорсов, требуемых каждой группой, и ресурсов, доступных на узле 
->
Агент Kubelet на узлах поручает среде запустить контейнеры.

![[BaseViewOfKuberWithApplication.png]]
*Базовый вид архитектуры Kubernetes и приложения, работающего поверх нее*

В описании приложения перечислены четыре контейнера, сгруппированных в три набора (эти наборы называются _модулями_ (подами)). Первые два модуля содержат всего один контейнер, тогда как последний содержит два. Это означает, что оба контейнера должны работать совместно и не должны быть изолированы друг от друга. Рядом с каждым модулем также можно увидеть число, представляющее количество реплик каждого модуля, которые должны выполняться параллельно.
После отправки дескриптора в Kubernetes он запланирует использование указанного количества реплик каждого модуля на доступных рабочих узлах. Затем агенты Kubelet на узлах поручают Docker извлечь образы контейнеров из реестра образов и запустить контейнеры.
___
# 4. Обеспечение бесперебойной работы контейнеров
После запуска приложения система Kubernetes постоянно следит за тем, чтобы то, в каком состоянии развернуто, всегда соответствовало указанному вами описанию.
Если один из экземпляров перестает работать должным образом, например когда происходит аварийный сбой процесса или он перестает отвечать на запросы, Kubernetes автоматически его перезапустит.
Аналогичным образом, если весь рабочий узел умирает или становится недоступным, Kubernetes выберет новые узлы для всех контейнеров, запущенных на узле, и запустит их на вновь выбранных узлах.
___
# 5. Services
По умолчанию каждый под получает свой собственный IP-адрес. но он является внутренним (Cluster IP).
Для того чтобы сделать модуль доступным извне, нужно предоставить к нему доступ через объект службы (Service), создать специальную службу с типом LoadBalancer. При создании службы с типом LoadBalancer будет создана внешняя подсистема балансировки нагрузки, и вы сможете подключаться к модулю через общедоступный IP-адрес подсистемы балансировки нагрузки.
Pod'ы имеют свойство "путешествовать" по кластеру. Это может быть вызвано, например, аварийным отказом ноды, на которой был размещен pod или перераспределением нагрузки на ноды, когда k8s пытается оптимизировать процессы. В таком случае у вновь поднятого pod'а изменится IP. Разрешает проблемы изменчивости IP-адресов pod'ов как раз объект службы. Сама служба имеет статически IP-адрес, потому все подключения должны идти именно через нее, а она уже сама распределит подключение к Pod'ам. Также распределит эти подключения, распределив нагрузку в случае, если replica set > 1.
___
# 6. Контроллер репликации
Базово контроллер репликации отвечает за количество pod'ов. Можно задать любое значение, и контроллер репликации будет стараться, чтобы количество pod'ов с определенным приложением было заданным.
___
# Литература
- Марко Лукша – Kubernetes в действии