___
___
# Tags
#kubernetes
___
# Содержание
- [[#1. Использование тома configMap для обеспечения доступа к записям словаря конфигурации в виде файлов]]
- [[#2. Использование записей словаря конфигурации в томе]]
- [[#3. Монтирование каталога скрывает существующие файлы]]
- [[#4. Монтирование отдельных записей словаря конфигурации в виде файлов без скрытия других файлов в каталоге]]
	- [[#4.1. Настройка прав для файлов в томе configMap]]
- [[#5. Обновление конфигурации приложения без перезапуска приложения]]
- [[#Литература]]
___
# 1. Использование тома configMap для обеспечения доступа к записям словаря конфигурации в виде файлов
Том `configMap` будет предоставлять доступ к каждой записи словаря конфигурации как файл. Процесс, запущенный в контейнере, может получить значение записи, прочитав содержимое файла.
Используем конфигурационный файл для настройки веб-сервера Nginx:
```nginx
server {
	listen 80;
	server_name www.kubia-example.com;
	gzip on;	
	gzip_types text/plain application/xml;
	location / {
		root /usr/share/nginx/html;
		index index.html index.htm;
	}
}
```
Создайте новую папку с именем `configmap-files` и сохраните конфигурационный файл Nginx в `configmap-files/my-nginx-config.conf`. Для того чтобы словарь конфигурации также содержал запись `sleep-interval`, добавьте текстовый файл `sleep-interval` в тот же каталог и сохраните в нем число 25.
Теперь создайте словарь конфигурации из всех файлов в каталоге, как показано ниже:
```shell
kubectl create configmap fortune-config --from-file=configmap-files
```
YAML этого словаря конфигурации:
```yaml
apiVersion: v1
data:
  my-nginx-config.conf: |
    server {
        listen 80;
		server_name www.kubia-example.com;
		gzip on;
		gzip_types text/plain application/xml;
		location / {
			root /usr/share/nginx/html;
			index index.html index.htm;
		}
	}
  sleep-interval: |
	25
kind: ConfigMap
...
```
___
# 2. Использование записей словаря конфигурации в томе
Веб-сервер Nginx читает свой конфигурационный файл из `/etc/nginx/nginx.conf`. Образ Nginx уже содержит этот файл с параметрами конфигурации по умолчанию, которые вы не хотите переопределять, поэтому вы не будете заменять этот файл целиком. К счастью, конфигурационный файл по умолчанию автоматически также включает все файлы `.conf` в подкаталоге `/etc/nginx/conf.d/`, поэтому вы должны добавить туда свой конфигурационный файл.
```yaml
apiVersion: v1
kind: Pod
metadata:
  name: fortune-configmap-volume
spec:
  containers:
    – image: nginx:alpine
      name: web-server
      volumeMounts:
      ...
      – name: config
        mountPath: /etc/nginx/conf.d # |<- монтирование тома
        readOnly: true
    ...
  volumes:
...
    – name: config           # |<- том ссылается на словарь ConfigMap
      configMap:             # | с именем fortune-config
        name: fortune-config # |__
...
```
___
# 3. Монтирование каталога скрывает существующие файлы
Стандартное монтирование каталогов затирает уже имеющиеся файлы. Т.е. если монтировать, например, в `/etc`, то все файлы, которые были там по умолчанию, станут недоступны.
___
# 4. Монтирование отдельных записей словаря конфигурации в виде файлов без скрытия других файлов в каталоге
Дополнительное свойство `subPath` в `volumeMount` позволяет монтировать либо один файл, либо один каталог из тома вместо монтирования всего тома.

Предположим, имеется том `configMap`, содержащий файл `myconfig.conf`, который вы хотите добавить в каталог `/etc` как `someconfig.conf`. Вы можете задействовать свойство `subPath`, для того чтобы смонтировать его, не затрагивая другие файлы в этом каталоге. Соответствующая часть определения модуля показана в следующем ниже листинге.

```yaml
spec:
  containers:
  – image: some/image
    volumeMounts:
    – name: myvolume
      mountPath: /etc/someconfig.conf # |<- монтируется файл, а не каталог
      subPath: myconfig.conf          # |<- вместо монтирования всего тома монтируется только запись myconfig.conf
```
Свойство `subPath` можно использовать при монтировании любого типа тома. Вместо монтирования всего тома можно смонтировать его часть. Но этот способ монтирования отдельных файлов имеет относительно большой недостаток, связанный с обновлением файлов.
## 4.1. Настройка прав для файлов в томе configMap
По умолчанию разрешения для всех файлов в томе `configMap` имеют значение 
`644` (`-rw-r--r--`). Это можно изменить, установив свойство `defaultMode` в спецификации тома:
```yaml
volumes:
- name: config
  configMap:
    name: fortune-config
    defaultMode: "6600"
```
___
# 5. Обновление конфигурации приложения без перезапуска приложения

___
# Литература
- Марко Лукша – Kubernetes в действии
